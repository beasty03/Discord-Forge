# DiscordForge Flask app dependencies
# Install with: pip install -r requirements.txt

# Core
Flask>=3.0.0
Werkzeug>=3.0.0
requests>=2.28.0

# Security / auth
cryptography>=41.0.0
Flask-Limiter>=4.0.0

# WebSocket (agent communication)
flask-socketio>=5.3.0
gevent>=23.9.0
gevent-websocket>=0.10.1

# WSGI (kept for local dev without gevent)
waitress>=3.0.0

# Docker SDK — used when BOT_RUNNER=docker to manage bot containers
docker>=7.0.0
